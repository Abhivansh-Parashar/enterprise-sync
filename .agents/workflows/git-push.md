---
description: Push changes to git after every code modification
---

After completing any code changes, always push to git:

// turbo
1. Stage all changes: `git add -A`

// turbo
2. Commit with a descriptive message: `git commit -m "<descriptive commit message>"`

3. Push to remote: `git push`
